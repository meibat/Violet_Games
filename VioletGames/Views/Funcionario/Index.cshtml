@model List<FuncionarioModel>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Violet_Games";
}

@if (TempData["MessagemSucess"] != null)
{
    <div class="alert alert-success text-center justify-content-center" role="alert">
        @TempData["MessagemSucess"]
        <button type="button" class="btn-close alert-success" data-bs-dismiss="alert" aria-label="Close">X</button>
    </div>
}

@if(TempData["MessagemError"] != null)
{
    <div class="text-center alert alert-danger justify-content-center" role="alert">
        @TempData["MessagemError"]
        <button type="button" class="btn-close alert-danger" data-bs-dismiss="alert" aria-label="Close">X</button>
    </div>
}


<div class="text-center"><br />
    <table class="table table-content" id="table-funcionarios">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">Data Nasc.</th>
                <th scope="col">E-mail</th>
                <th scope="col">CPF</th>
                <th scope="col">RG</th>
                <th scope="col">Admissão</th>
                <th scope="col">Cargo</th>
                <th scope="col">Salário</th>
                <th scope="col">Contato</th>
                <th scope="col">
                    <a role="button" class="btn btn-success" asp-controller="Funcionario" asp-action="Create"><img src="~/lib/bottons/add.png" width="30" height="30"></a>
                </th>
            </tr>
        </thead>
        <tbody>
            <!--Listar contatos SE existir-->
            @if (Model != null && Model.Any())
            {
                foreach (FuncionarioModel funcionario in Model)
                {
            <tr>
                <th scope="row">@funcionario.Id</th>
                <td>@funcionario.Name</td>
                <td>@funcionario.DateBirthday</td>
                <td>@funcionario.Email</td>
                <td>@funcionario.CPF</td>
                <td>@funcionario.RG</td>
                <td>@funcionario.DateAdmission</td>
                <td>@(funcionario.Office == VioletGames.Enums.Office.Boss ? "Gerente" : "Atendente")</td>
                <td>@funcionario.Pay</td>
                <td>@funcionario.Contato</td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic mixed styles example">
                        <a role="button" class="btn btn-warning" asp-route-id="@funcionario.Id" asp-controller="Funcionario" asp-action="Edit"><img src="~/lib/bottons/edit.png" width="20" height="20"></a>
                        <a role="button" class="btn btn-danger" asp-route-id="@funcionario.Id" asp-controller="Funcionario" asp-action="DeleteConfirm"><img src="~/lib/bottons/Del.png" width="25" height="25"></a>
                    </div>
                </td>
            </tr>
                }
            }
        </tbody>
    </table>
</div>

